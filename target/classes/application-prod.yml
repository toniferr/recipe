spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_pro?allowPublicKeyRetrieval=true?useSSL=false&serverTimezone=UTC
    username: recipe_prod_user
    password: toni
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false